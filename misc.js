//Using Array constructor, fill() fills the array with zeros
var sequence = new Array(10).fill(0);

console.log('An array with ten 0: ' + sequence);

//Pick random from { 0, 1, 2, 3 }
var randomNumber = Math.floor(Math.random() * 4);

console.log('Picking a random number from { 0, 1, 2, 3, 4 }: ' + randomNumber);